#include <stdio.h>

#define SIZE 5;

int n, arrays[SIZE], getint(int *);
int main()
{
   for( n =0 ; (  (n < SIZE)&&(getint(&arrays[n])!= EOF )  ); n++)
   {
       ;
   }

   return 0;
}

int getint(int *p)
{
    return 0;
}
